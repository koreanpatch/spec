{
  "private": true,
  "packageManager": "pnpm@10.28.1",
  "type": "module",
  "scripts": {
    "generate": "pnpm -r --filter spec-sdk generate",
    "build": "pnpm -r build",
    "dev": "pnpm --filter spec-server dev"
  },
  "pnpm": {
    "onlyBuiltDependencies": ["esbuild", "bcrypt", "sharp"]
  },
  "devDependencies": {
    "typescript": "^5.7.3",
    "tsx": "^4.19.2",
    "@types/node": "^22.12.0"
  }
}
