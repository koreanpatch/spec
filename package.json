{
  "private": true,
  "packageManager": "pnpm@10.28.1",
  "type": "module",
  "scripts": {
    "generate": "pnpm -r --filter spec-sdk generate",
    "build": "pnpm -r build",
    "typecheck": "pnpm -r --if-present run typecheck",
    "test": "pnpm -r --if-present run test"
  },
  "pnpm": {
    "onlyBuiltDependencies": ["esbuild", "bcrypt", "sharp"]
  },
  "devDependencies": {
    "typescript": "^5.7.3",
    "tsx": "^4.19.2",
    "@types/node": "^22.12.0"
  }
}
